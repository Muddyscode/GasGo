# Verification evidence

Proof artifacts from `verify-gasgo` live here. `helpers/cleanup.sh` deletes `.cursor/skills/verify-gasgo/.run/` only — **never this folder**.

Expected after a marketing-landing drive:

- `marketing-landing-<stamp>-01-landing.png` + `.json` — hero before the click
- `marketing-landing-<stamp>-02-fill.png` + `.json` — `/order/cylinder` after **Start a refill**
- `marketing-landing-<stamp>-report.json`
- `LAST_PROOF.json` — pointer at the newest report
